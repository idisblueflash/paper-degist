# Python
__pycache__/
*.py[cod]
.pytest_cache/

# uv / virtualenv
.venv/

# Project output
files/
manifest.jsonl

# OCR bench artifacts (US19 render-pdf pages, US20 ocr-page Markdown)
pages/
out/

# US18 browser lane: the dev-mode Chrome profile holds live authenticated
# sessions (secrets-at-rest) — never commit it.
.browser-profile/

# bureau: rendered gazette (build artifact)
/gazette/
