# Python
__pycache__/
*.py[cod]
.pytest_cache/

# uv / virtualenv
.venv/

# Secrets / API keys — copy .env.example to .env and fill it in; never commit it.
.env

# Project output
files/
manifest.jsonl
scores.jsonl

# OCR bench artifacts: rendered page images (US19) and per-model OCR output (US20)
pages/
out/

# US18 browser lane: the dev-mode Chrome profile holds live authenticated
# sessions (secrets-at-rest) — never commit it.
.browser-profile/

# bureau: rendered gazette (build artifact)
/gazette/
