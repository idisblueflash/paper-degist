---
title: session a8521e68-7693-4af4-9f5e-d179bf13c735 · 2026-07-03
updated: 2026-07-03
status: logbook
session: a8521e68-7693-4af4-9f5e-d179bf13c735
transcript: "/Users/husongtao/.claude/projects/-Users-husongtao-Projects-paper-degist/a8521e68-7693-4af4-9f5e-d179bf13c735.jsonl"
---

## [2026-07-03T13:40:00Z] session a8521e68 — finish rule06 phase 14, ship bureau, trace markdown corruption to VMark

**Intent.** Began with the user asking why master wasn't switched back to after a
merge; became: finish rule 06 phase 14 (post-merge cleanup), prune stale branches,
land the bureau workspace, and diagnose why spec files kept getting corrupted.

**Decisions.**
- Rule 06 **phase 14** (post-merge cleanup: switch → `pull --ff-only` → `branch -d`
  → delete remote, with step-0 "clean tree first") is the process fix for the
  stale-branch gap that prompted the question — implies dossier **Feature workflow (rule 06)**.
- **Discard, don't stash, VMark churn across a fast-forward.** Incoming master edited
  `user-stories.md`, so a stash-pop would hit the exact silent-mangle hazard phase-14
  step 0 warns about; WIP was moved to its own branch instead — implies dossier **Feature workflow (rule 06)**.
- The `user-stories.md` table reflow is **VMark churn, not intentional work** → dropped
  it from PR #13 (resolved the conflict by taking master's version); PR #13 ships only
  bureau scaffolding — implies dossier **Editor tooling / markdown corruption**.
- Root cause of recurring `.md` corruption is **VMark.app re-serializing open files on
  save** (injects `&#xA;`/`&#x20;` entities, reflows tables, adds stray bold) — implies
  dossier **Editor tooling / markdown corruption**. User closed the VMark tab to stop it.

**Changes.**
- `.claude/rules/06-feature-workflow.md` (updated) — phase 14 added + emphasis typo
  (`*first_`→`*first*`) fixed; merged to master via **PR #12**.
- `BUREAU.md`, `canon/` (new) + `CLAUDE.md` `@BUREAU.md` import — bureau workspace;
  merged to master via **PR #13**.
- Branches pruned local+remote: `docs/status-flip-in-pr`, `feat/parse-url`,
  `docs/rule06-post-merge-cleanup`, `chore/bureau-init`.

**Open threads.**
- **Committed corruption to fix:** `user-stories/us-15-browser-fetch-bot-walled.md:26`
  carries `&#x20;` entities in HEAD, landed in commit `96ac1b9`; audit the rest of that
  commit for entity/reflow/bold damage `git grep '&#x'` won't fully catch.
- **Residual working state:** `stash@{0}` ("vmark-churn (discard)") and a dirty
  working-tree `user-stories.md` remain — safe to drop once VMark stays closed.
- **PR #14** (`docs/rule09-remote-dev-macmini`, rule 09) is open from a parallel
  machine — not touched here.

**Source.** transcript `/Users/husongtao/.claude/projects/-Users-husongtao-Projects-paper-degist/a8521e68-7693-4af4-9f5e-d179bf13c735.jsonl`
